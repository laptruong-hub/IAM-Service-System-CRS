server:
  port: 8080

spring:
  application:
    name: iam-service

  # Mail Configuration
  mail:
    host: smtp.gmail.com
    port: 587
    username: carrentalsystem1806@gmail.com
    password: ztia zfoz faay sfky
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  # Thymeleaf Configuration
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
    cache: false

  datasource:
    # 'postgres-db' là tên service chúng ta đặt trong docker-compose.yml
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/iam_service_db}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:123456}

  jpa:
    hibernate:
      ddl-auto: update
      open-in-view: false
    show-sql: true

application:
  security:
    jwt:
      secret-key: CceAYTEK9XB5w/ibsdi5pvuNynAwNfDvGI7y+BD3ziA=
      expiration: 86400000 # 1 ngày
      refresh-token:
        expiration: 604800000       # Refresh Token: 7 ngày

eureka:
  client:
    enabled: false
